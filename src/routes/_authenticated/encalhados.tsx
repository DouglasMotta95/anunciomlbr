import { createFileRoute,Link } from '@tanstack/react-router';import { AlertTriangle,Clock3,HeartPulse } from 'lucide-react';import { AppShell } from '@/components/app/AppShell';import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card';import { Button } from '@/components/ui/button';import { Badge } from '@/components/ui/badge';import { useListings } from '@/hooks/useLicense';
export const Route=createFileRoute('/_authenticated/encalhados')({head:()=>({meta:[{title:'Anúncios encalhados — ANÚNCIO ML'},{name:'robots',content:'noindex'}]}),component:Page});
function Page(){const{data:rows=[],isLoading}=useListings(),now=Date.now(),stale=rows.filter((x:any)=>x.status==='active'&&x.updated_at&&now-new Date(x.updated_at).getTime()>=30*86400000);return <AppShell title="Anúncios encalhados" description="Triagem conservadora de anúncios ativos sem atualização recente para revisão comercial."><Card><CardHeader><div className="flex items-center justify-between"><CardTitle className="text-base">Revisar anúncios</CardTitle><Badge variant="outline">{isLoading?'…':stale.length}</Badge></div></CardHeader><CardContent className="space-y-2">{!isLoading&&!stale.length?<div className="py-12 text-center"><Clock3 className="mx-auto h-8 w-8 text-primary"/><p className="mt-3 font-semibold">Nenhum candidato encontrado</p><p className="mt-1 text-sm text-muted-foreground">Sem histórico completo de vendas por anúncio, não afirmamos que um item está sem vender.</p></div>:stale.map((x:any)=><div key={x.id} className="flex flex-col gap-3 rounded-xl border p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">{x.title}</p><p className="mt-1 text-xs text-muted-foreground">Sem atualização local há pelo menos 30 dias. Isso é um sinal para revisão, não prova de ausência de vendas.</p></div><Button asChild size="sm"><Link to="/editor/$id" params={{id:x.id}}><HeartPulse className="mr-2 h-4 w-4"/>Revisar</Link></Button></div>)}</CardContent></Card><div className="mt-4 flex items-start gap-2 rounded-xl border p-4 text-sm text-muted-foreground"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0"/>Quando houver histórico de pedidos suficiente, esta triagem poderá usar venda real por anúncio em vez de atualização local.</div></AppShell>}