import { createFileRoute, Link } from "@tanstack/react-router";
import { AlertTriangle, CheckCircle2, HeartPulse, Image, ListChecks, PackageCheck, RefreshCcw, Tag, Text } from "lucide-react";
import { AppShell } from "@/components/app/AppShell";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { Skeleton } from "@/components/ui/skeleton";
import { useListings } from "@/hooks/useLicense";
import { calculateListingHealth } from "@/lib/listing-health";
export const Route=createFileRoute("/_authenticated/saude-anuncios")({head:()=>({meta:[{title:"Raio-X dos anúncios — ANÚNCIO ML"},{name:"robots",content:"noindex"}]}),component:ListingHealthPage});
function componentScores(listing:any){const title=String(listing.title??"").trim(),description=String(listing.description??"").trim(),images=Array.isArray(listing.images)?listing.images:[],attrs=Array.isArray(listing.attributes)?listing.attributes:listing.attributes&&typeof listing.attributes==="object"?Object.keys(listing.attributes):[];return [{label:"Título",value:title.length>=30&&title.length<=60?100:title.length>=15?60:25,icon:Tag},{label:"Imagens",value:Math.min(100,images.length*20),icon:Image},{label:"Descrição",value:description.length>=500?100:description.length>=200?60:20,icon:Text},{label:"Ficha técnica",value:attrs.length>=5?100:attrs.length>=2?55:15,icon:ListChecks},{label:"Oferta",value:Number(listing.price_cents??0)>0&&Number(listing.stock??0)>0?100:40,icon:PackageCheck}]}
function ListingHealthPage(){const{data:listings=[],isLoading,isError,error,refetch,isFetching}=useListings(),rows=listings.map(listing=>({listing,health:calculateListingHealth(listing)})).sort((a,b)=>a.health.score-b.health.score),average=rows.length?Math.round(rows.reduce((s,r)=>s+r.health.score,0)/rows.length):0,critical=rows.filter(r=>r.health.status==="critico").length,good=rows.filter(r=>["bom","excelente"].includes(r.health.status)).length;return <AppShell title="Raio-X dos anúncios" description="Diagnóstico organizado de título, imagens, descrição, ficha técnica, preço e estoque.">{isLoading?<div className="space-y-4"><div className="grid gap-4 sm:grid-cols-3">{Array.from({length:3}).map((_,i)=><Skeleton key={i} className="h-24 rounded-2xl"/>)}</div><Skeleton className="h-52 rounded-2xl"/></div>:isError?<Card><CardContent className="flex flex-col items-center gap-3 py-12 text-center"><AlertTriangle className="h-8 w-8 text-destructive"/><p className="font-semibold">Não foi possível analisar seus anúncios</p><p className="max-w-md text-sm text-muted-foreground">{error instanceof Error?error.message:"Os anúncios não puderam ser carregados agora."}</p><Button size="sm" variant="outline" disabled={isFetching} onClick={()=>void refetch()}><RefreshCcw className="mr-2 h-4 w-4"/>Tentar novamente</Button></CardContent></Card>:<><div className="grid gap-4 sm:grid-cols-3"><Metric label="Score médio" value={`${average}/100`}/><Metric label="Críticos" value={String(critical)}/><Metric label="Bons ou excelentes" value={String(good)}/></div><div className="mt-4 space-y-3">{rows.length===0?<Card><CardContent className="py-12 text-center"><HeartPulse className="mx-auto h-8 w-8 text-muted-foreground"/><p className="mt-3 font-semibold">Nenhum anúncio para analisar</p><p className="mt-1 text-sm text-muted-foreground">Sincronize sua conta ou crie um anúncio para começar.</p><Button asChild size="sm" className="mt-4"><Link to="/buscar">Buscar anúncios</Link></Button></CardContent></Card>:rows.map(({listing,health})=><Card key={listing.id}><CardHeader className="pb-3"><div className="flex flex-wrap items-start justify-between gap-3"><div className="min-w-0"><CardTitle className="truncate text-base">{listing.title}</CardTitle><p className="mt-1 text-xs text-muted-foreground">{listing.source_ml_id??listing.published_ml_id??"Rascunho local"}</p></div><Badge variant={health.status==="critico"?"destructive":health.status==="atencao"?"secondary":"default"}>{health.score}/100 · {health.status==="atencao"?"atenção":health.status}</Badge></div></CardHeader><CardContent><Progress value={health.score}/><div className="mt-4 grid gap-2 sm:grid-cols-5">{componentScores(listing).map(part=><div key={part.label} className="rounded-xl border p-3"><div className="flex items-center gap-2 text-xs text-muted-foreground"><part.icon className="h-3.5 w-3.5"/>{part.label}</div><p className="mt-2 font-display text-xl font-bold">{part.value}<span className="text-xs font-normal text-muted-foreground">/100</span></p><Progress className="mt-2 h-1.5" value={part.value}/></div>)}</div>{health.fixes.length>0?<div className="mt-4 grid gap-2 md:grid-cols-2">{health.fixes.map((fix,index)=><div key={`${fix}-${index}`} className="flex items-start gap-2 rounded-xl bg-muted/50 p-3 text-sm"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0 text-muted-foreground"/><span>{fix}</span></div>)}</div>:<div className="mt-4 flex items-center gap-2 text-sm text-muted-foreground"><CheckCircle2 className="h-4 w-4 text-primary"/>Nenhuma correção básica pendente.</div>}<div className="mt-4 flex flex-wrap gap-2"><Button asChild size="sm"><Link to="/editor/$id" params={{id:listing.id}}><HeartPulse className="mr-2 h-4 w-4"/>Corrigir anúncio</Link></Button><Button asChild size="sm" variant="outline"><Link to="/mercado">Comparar mercado</Link></Button></div></CardContent></Card>)}</div></>}</AppShell>}
function Metric({label,value}:{label:string;value:string}){return <Card><CardContent className="pt-6"><p className="text-xs uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-2 font-display text-2xl font-bold">{value}</p></CardContent></Card>}
