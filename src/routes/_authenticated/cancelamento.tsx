import { createFileRoute, Link } from '@tanstack/react-router'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useServerFn } from '@tanstack/react-start'
import { CheckCircle2, Loader2, ShieldCheck } from 'lucide-react'
import { useState } from 'react'
import { toast } from 'sonner'
import { AppShell } from '@/components/app/AppShell'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Textarea } from '@/components/ui/textarea'
import { formatDate } from '@/lib/format'
import { getCancellationState, requestCancellation, withdrawCancellation } from '@/lib/cancellation.functions'
import { retentionAlternatives } from '@/lib/growth'
export const Route=createFileRoute('/_authenticated/cancelamento')({component:Cancelamento})
const reasons=[['price','O preço não compensa para mim'],['usage','Estou usando pouco'],['missing','Falta um recurso que preciso'],['technical','Tive problemas técnicos'],['other','Outro motivo']] as const
function Cancelamento(){const stateFn=useServerFn(getCancellationState),requestFn=useServerFn(requestCancellation),withdrawFn=useServerFn(withdrawCancellation),qc=useQueryClient();const {data,isLoading}=useQuery({queryKey:['cancellation-state'],queryFn:()=>stateFn()});const [reason,setReason]=useState(''),[details,setDetails]=useState('');const alternatives=retentionAlternatives(reason);const request=useMutation({mutationFn:()=>requestFn({data:{reason:reason as any,details:details||null}}),onSuccess:r=>{toast.success(r.already_requested?'Seu pedido já estava registrado.':'Pedido de cancelamento registrado.',{description:r.access_until?`Seu acesso permanece disponível até ${formatDate(r.access_until)}.`:undefined});qc.invalidateQueries({queryKey:['cancellation-state']})},onError:e=>toast.error(e instanceof Error?e.message:'Não foi possível registrar o pedido.')});const withdraw=useMutation({mutationFn:()=>withdrawFn(),onSuccess:()=>{toast.success('Pedido de cancelamento retirado.');qc.invalidateQueries({queryKey:['cancellation-state']})}});if(isLoading)return <AppShell title="Gerenciar assinatura"><div className="h-40 animate-pulse rounded-xl bg-muted"/></AppShell>;if(data?.request)return <AppShell title="Gerenciar assinatura" description="Seu pedido está registrado e sua assinatura continua utilizável até o encerramento do período atual."><div className="mx-auto max-w-2xl"><Card className="border-amber-500/30"><CardHeader><CardTitle>Cancelamento solicitado</CardTitle></CardHeader><CardContent className="space-y-4"><div className="rounded-xl bg-muted/40 p-4 text-sm"><p><strong>Solicitado em:</strong> {formatDate(data.request.requested_at)}</p><p><strong>Motivo:</strong> {reasons.find(r=>r[0]===data.request.reason)?.[1]??data.request.reason}</p>{data.license?.expires_at&&<p><strong>Acesso previsto até:</strong> {formatDate(data.license.expires_at)}</p>}</div><p className="text-sm text-muted-foreground">Se você mudou de ideia, pode retirar o pedido. Não criamos obstáculos para cancelamento.</p><Button onClick={()=>withdraw.mutate()} disabled={withdraw.isPending}>{withdraw.isPending&&<Loader2 className="mr-2 h-4 w-4 animate-spin"/>Retirar pedido de cancelamento</Button></CardContent></Card></div></AppShell>;return <AppShell title="Gerenciar assinatura" description="Você mantém o controle. Podemos tentar resolver o motivo, mas o pedido de cancelamento continua disponível."><div className="mx-auto max-w-2xl space-y-4"><Card><CardHeader><CardTitle>Antes de cancelar, o que aconteceu?</CardTitle></CardHeader><CardContent className="space-y-2">{reasons.map(([key,label])=><button key={key} onClick={()=>setReason(key)} className={`w-full rounded-xl border p-3 text-left text-sm ${reason===key?'border-primary bg-primary/5':''}`}>{label}</button>)}{reason&&<Textarea value={details} onChange={e=>setDetails(e.target.value)} placeholder="Quer explicar um pouco mais? (opcional)" className="mt-3"/>}</CardContent></Card>{reason&&<Card><CardHeader><CardTitle>Antes de sair, veja se alguma opção resolve</CardTitle></CardHeader><CardContent className="space-y-2">{alternatives.map(a=><Button key={a.key} asChild variant="outline" className="w-full justify-start"><Link to={a.to==='/planos'?'/licenca':a.to as any}><ShieldCheck className="mr-2 h-4 w-4"/>{a.label}</Link></Button>)}<div className="my-4 border-t"/><Button variant="destructive" className="w-full" disabled={request.isPending} onClick={()=>request.mutate()}>{request.isPending?<Loader2 className="mr-2 h-4 w-4 animate-spin"/>:<CheckCircle2 className="mr-2 h-4 w-4"/>}Confirmar pedido de cancelamento</Button><p className="pt-2 text-xs text-muted-foreground">Registrar o pedido não apaga seus dados e não interrompe o período já pago. O processamento financeiro depende do meio de cobrança e das regras da assinatura.</p></CardContent></Card>}</div></AppShell>}
