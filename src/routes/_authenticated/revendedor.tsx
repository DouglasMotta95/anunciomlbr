import { createFileRoute } from "@tanstack/react-router";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-start";
import { Copy, KeyRound, Loader2, Store, Users, WalletCards } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { AppShell } from "@/components/app/AppShell";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { usePlans } from "@/hooks/usePlans";
import { formatBRL, formatDateTime } from "@/lib/format";
import { getResellerProfessionalSummary, resellerIssueLicense } from "@/lib/reseller.functions";
import { getResellerDashboard } from "@/lib/seller-growth.functions";
import type { BillingPeriod } from "@/lib/pricing";
export const Route=createFileRoute("/_authenticated/revendedor")({head:()=>({meta:[{title:"Painel do revendedor — ANÚNCIO ML"},{name:"robots",content:"noindex"}]}),component:ResellerPage});
function ResellerPage(){const dashboardFn=useServerFn(getResellerDashboard),professionalFn=useServerFn(getResellerProfessionalSummary),issueFn=useServerFn(resellerIssueLicense);const {data,isLoading}=useQuery({queryKey:["reseller-dashboard"],queryFn:()=>dashboardFn()});const {data:pro}=useQuery({queryKey:["reseller-professional"],queryFn:()=>professionalFn()});const {data:plans=[]}=usePlans(),qc=useQueryClient();const [planId,setPlanId]=useState(""),[period,setPeriod]=useState<BillingPeriod>("monthly"),[customerEmail,setCustomerEmail]=useState(""),[customerName,setCustomerName]=useState(""),[lastCode,setLastCode]=useState<string|null>(null);const issue=useMutation({mutationFn:()=>issueFn({data:{plan_id:planId,period,customer_email:customerEmail||null,customer_name:customerName||null}}),onSuccess:r=>{setLastCode(r.license_code);qc.invalidateQueries({queryKey:["reseller-dashboard"]});qc.invalidateQueries({queryKey:["reseller-professional"]});toast.success("Licença emitida")},onError:e=>toast.error(e instanceof Error?e.message:"Falha ao emitir licença")});if(isLoading)return <AppShell title="Painel do revendedor"><p>Carregando…</p></AppShell>;if(!data?.enabled)return <AppShell title="Painel do revendedor"><Card><CardContent className="py-10 text-center"><Store className="mx-auto h-8 w-8 text-muted-foreground"/><p className="mt-3 font-semibold">Seu cadastro ainda não está habilitado como revendedor.</p></CardContent></Card></AppShell>;return <AppShell title="Painel do revendedor" description="Saldo pré-pago, emissão de licenças, clientes e histórico financeiro."><div className="grid gap-4 md:grid-cols-3"><Metric label="Saldo disponível" value={formatBRL(data.reseller.wallet_cents)}/><Metric label="Vendas acumuladas" value={formatBRL(data.reseller.total_sales_cents)}/><Metric label="Margem acumulada" value={formatBRL(data.reseller.total_commission_cents)}/></div><div className="mt-4 grid gap-4 lg:grid-cols-3"><Card><CardHeader><CardTitle className="text-base">Emitir licença</CardTitle></CardHeader><CardContent className="space-y-3"><div><Label>Cliente (opcional)</Label><Input value={customerName} onChange={e=>setCustomerName(e.target.value)} placeholder="Nome do cliente" className="mb-2"/><Input type="email" value={customerEmail} onChange={e=>setCustomerEmail(e.target.value)} placeholder="cliente@email.com"/></div><div><Label>Plano</Label><Select value={planId} onValueChange={setPlanId}><SelectTrigger><SelectValue placeholder="Selecione"/></SelectTrigger><SelectContent>{plans.map(p=><SelectItem key={p.id} value={p.id}>{p.name} · {formatBRL(p.price_monthly_cents)}/mês</SelectItem>)}</SelectContent></Select></div><div><Label>Período</Label><Select value={period} onValueChange={v=>setPeriod(v as BillingPeriod)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="monthly">Mensal</SelectItem><SelectItem value="quarterly">3 meses</SelectItem><SelectItem value="semiannual">6 meses</SelectItem><SelectItem value="annual">Anual</SelectItem></SelectContent></Select></div><div className="rounded-xl bg-muted/50 p-3 text-xs text-muted-foreground">Seu custo usa {Number(data.reseller.discount_percent)}% de desconto e é descontado do saldo no momento da emissão.</div><Button className="w-full" disabled={!planId||issue.isPending} onClick={()=>issue.mutate()}>{issue.isPending&&<Loader2 className="mr-2 h-4 w-4 animate-spin"/>}<KeyRound className="mr-2 h-4 w-4"/>Gerar licença</Button>{lastCode&&<div className="rounded-xl border border-primary/20 bg-primary/5 p-3"><p className="text-xs text-muted-foreground">Última licença</p><div className="mt-1 flex items-center justify-between"><code className="font-bold">{lastCode}</code><Button size="icon" variant="ghost" onClick={()=>navigator.clipboard.writeText(lastCode)}><Copy className="h-4 w-4"/></Button></div></div>}</CardContent></Card><Card className="lg:col-span-2"><CardHeader><CardTitle className="text-base">Histórico de vendas</CardTitle></CardHeader><CardContent className="overflow-x-auto">{data.sales.length===0?<p className="text-sm text-muted-foreground">Nenhuma licença emitida ainda.</p>:<Table><TableHeader><TableRow><TableHead>Data</TableHead><TableHead>Plano</TableHead><TableHead>Venda sugerida</TableHead><TableHead>Seu custo</TableHead><TableHead>Margem</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{data.sales.map((s:any)=><TableRow key={s.id}><TableCell className="text-xs">{formatDateTime(s.created_at)}</TableCell><TableCell>{s.plans?.name??"—"}</TableCell><TableCell>{formatBRL(s.sale_price_cents)}</TableCell><TableCell>{formatBRL(s.reseller_cost_cents)}</TableCell><TableCell>{formatBRL(s.commission_cents)}</TableCell><TableCell><Badge variant={s.status==="completed"?"default":"outline"}>{s.status==="completed"?"Concluída":s.status}</Badge></TableCell></TableRow>)}</TableBody></Table>}</CardContent></Card></div><div className="mt-4 grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Users className="h-4 w-4 text-primary"/>Clientes vinculados</CardTitle></CardHeader><CardContent>{(pro?.customers??[]).length===0?<p className="text-sm text-muted-foreground">Nenhum cliente registrado.</p>:<div className="space-y-2">{pro?.customers.map((c:any)=><div key={c.id} className="rounded-xl border p-3"><p className="font-medium">{c.customer_name??c.customer_email}</p><p className="text-xs text-muted-foreground">{c.customer_email}</p><p className="mt-1 font-mono text-xs">Última licença: {c.last_license_code??"—"}</p></div>)}</div>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><WalletCards className="h-4 w-4 text-primary"/>Movimentações da carteira</CardTitle></CardHeader><CardContent>{(pro?.wallet??[]).length===0?<p className="text-sm text-muted-foreground">Nenhuma movimentação registrada.</p>:<div className="space-y-2">{pro?.wallet.map((w:any)=><div key={w.id} className="flex items-center justify-between rounded-xl border p-3"><div><p className="text-sm font-medium">{w.kind==="license_debit"?"Emissão de licença":"Ajuste de saldo"}</p><p className="text-xs text-muted-foreground">{formatDateTime(w.created_at)}</p></div><strong className={w.amount_cents<0?"text-destructive":"text-emerald-600"}>{w.amount_cents>0?'+':''}{formatBRL(w.amount_cents)}</strong></div>)}</div>}</CardContent></Card></div></AppShell>}
function Metric({label,value}:{label:string;value:string}){return <Card><CardContent className="pt-6"><p className="text-xs uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-2 font-display text-2xl font-bold">{value}</p></CardContent></Card>}
